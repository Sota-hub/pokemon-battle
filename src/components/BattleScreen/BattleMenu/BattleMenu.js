import classes from "./BattleMenu.module.scss";

const BattleMenu = () => {
  return (
    <div className={classes.menu_details}>
      <a href="#" className={classes.attack_choice}>
        Solar-Beam
      </a>
    </div>
  );
};

export default BattleMenu;
